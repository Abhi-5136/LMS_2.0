## How to Test

* Start the backend server: Open a terminal in backend and run `node server.js`

* Start the frontend server: Open a terminal in lms2 and run `npm run dev`

* Register/Login: Navigate to your frontend, register a new account, and login.

* Use the App: Check out the admin and user dashboards, add some books, and issue them!
